# 🔐 Google OAuth — Полная инструкция для продакшна
### Сайт: ingiteai.online | Бэкенд: Render | Фронтенд: Vercel

---

## 📋 Что у нас есть (уже сделано в коде)

- ✅ Фронтенд исправлен — теперь правильно читает Google токен (`credential`)
- ✅ Бэкенд настроен — добавлен `allauth.socialaccount.providers.google`
- ⬜ **Нужно сделать вами** — 3 шага ниже

---

## ШАГ 1 — Google Cloud Console (получаем ключи)

> 🌐 Сайт: **https://console.cloud.google.com**

### 1.1 Открыть Console
1. Откройте браузер и зайдите на [https://console.cloud.google.com](https://console.cloud.google.com)
2. Войдите в ваш **Google аккаунт**
3. В верхнем левом углу выберите ваш проект (или создайте новый)

### 1.2 Найти Credentials
1. В левом меню нажмите **"APIs & Services"** (Службы и приложения)
2. Затем нажмите **"Credentials"** (Учетные данные)
3. Вы увидите список. Найдите ваш **"OAuth 2.0 Client IDs"** и нажмите на него (карандаш ✏️ для редактирования)

### 1.3 Добавить продакшн URL-адреса

Найдите раздел **"Authorized JavaScript origins"** и добавьте эти два URL:
```
https://ingiteai.online
https://www.ingiteai.online
```

Найдите раздел **"Authorized redirect URIs"** и добавьте:
```
https://ingiteai.online/auth/callback
https://www.ingiteai.online/auth/callback
```

> ⚠️ **ВАЖНО:** После добавления нажмите кнопку **"Save"** (Сохранить) внизу страницы!

### 1.4 Скопировать ваши ключи

После сохранения вы увидите:
- **Client ID** — длинная строка, заканчивается на `.apps.googleusercontent.com`
- **Client Secret** — начинается на `GOCSPX-...`

> 💡 Ваши ключи уже есть в `.env` файлах проекта. Просто убедитесь что они совпадают!
> - Client ID: `YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com`
> - Client Secret: `YOUR_GOOGLE_CLIENT_SECRET`

---

## ШАГ 2 — Django Admin на Render (настраиваем базу данных)

> 🌐 Ваш Admin: **https://ingite-backend.onrender.com/admin/**

### 2.1 Войти в Django Admin
1. Откройте `https://ingite-backend.onrender.com/admin/`
2. Введите логин/пароль суперпользователя
3. Если не помните пароль — создайте нового:
   - В Render → ваш сервис → **Shell** вкладка → введите команду:
   ```
   python manage.py createsuperuser
   ```

### 2.2 Настроить Site (сайт)
1. В Admin найдите раздел **"Sites"**
2. Нажмите **"Add site"** или отредактируйте существующий `example.com`
3. Заполните:
   - **Domain name**: `ingiteai.online`
   - **Display name**: `IngiteAI`
4. Нажмите **"Save"**
5. Запомните **ID** этого сайта (обычно 1 или 2, видно в URL при редактировании)

### 2.3 Установить правильный SITE_ID

> По умолчанию в `settings.py` стоит `SITE_ID = 1`.  
> Если ваш сайт `ingiteai.online` получил ID = 2, то нужно изменить на `SITE_ID = 2`.
> Проверить ID можно в Admin → Sites и посмотреть на URL при редактировании.

### 2.4 Добавить Social Application (Google)
1. В Admin найдите раздел **"Social Accounts"** → **"Social applications"**
2. Нажмите **"Add social application"**
3. Заполните форму:

| Поле | Значение |
|------|----------|
| Provider | `Google` |
| Name | `Google` |
| Client id | `YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com` |
| Secret key | `YOUR_GOOGLE_CLIENT_SECRET` |
| Sites | Выберите `ingiteai.online` из левого списка → нажмите стрелку → он перейдет в **"Chosen sites"** |

4. Нажмите **"Save"**

---

## ШАГ 3 — Vercel (переменные окружения фронтенда)

> 🌐 Сайт: **https://vercel.com**

### 3.1 Открыть настройки проекта
1. Зайдите на [https://vercel.com](https://vercel.com)
2. Войдите в аккаунт
3. Найдите ваш проект (IngiteAI фронтенд)
4. Нажмите **"Settings"** (вверху)
5. В левом меню нажмите **"Environment Variables"**

### 3.2 Добавить/проверить переменные

Убедитесь что есть эти переменные (если нет — добавьте):

| Ключ | Значение |
|------|----------|
| `NUXT_PUBLIC_API_BASE` | `https://ingite-backend.onrender.com` |
| `NUXT_PUBLIC_GOOGLE_CLIENT_ID` | `YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com` |

> ⚠️ **ВАЖНО:** `NUXT_PUBLIC_API_BASE` должен указывать на ваш Render URL, **НЕ на localhost!**
> Формат: `https://ingiteai-backend.onrender.com` (без слэша в конце)

### 3.3 Передеплоить фронтенд
После добавления переменных нужно передеплоить:
1. Зайдите в **Deployments** вашего проекта на Vercel
2. Нажмите **"Redeploy"** на последнем деплое
3. Или запушьте любое изменение в Git — Vercel сам передеплоит

---

## ШАГ 4 — Render (переменные окружения бэкенда)

> 🌐 Сайт: **https://render.com**

### 4.1 Открыть настройки сервиса
1. Зайдите на [https://render.com](https://render.com)
2. Выберите ваш бэкенд сервис
3. Нажмите **"Environment"** в левом меню

### 4.2 Проверить переменные
Убедитесь что есть:

| Ключ | Значение |
|------|----------|
| `FRONTEND_URL` | `https://ingiteai.online` |
| `GOOGLE_CLIENT_ID` | `YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com` |
| `GOOGLE_CLIENT_SECRET` | `YOUR_GOOGLE_CLIENT_SECRET` |
| `CORS_ALLOWED_ORIGINS` | `https://ingiteai.online,https://www.ingiteai.online` |
| `DEBUG` | `False` |

### 4.3 Передеплоить бэкенд
После изменения переменных Render автоматически передеплоит сервис.

---

## ✅ Финальная проверка

После всех шагов:
1. Откройте [https://ingiteai.online/login](https://ingiteai.online/login)
2. Нажмите **"Continue with Google"**
3. Должна открыться Google форма выбора аккаунта
4. После выбора — вы должны попасть в дашборд

### Если что-то не работает:
- Откройте DevTools (F12) в браузере → вкладку **Console** — там будут ошибки
- Проверьте вкладку **Network** → найдите запрос к `/api/v1/auth/google/` и посмотрите ответ

---

## 📞 Схема работы Google OAuth (для понимания)

```
Пользователь нажимает "Continue with Google"
        ↓
Открывается Google окно (vue3-google-login)
        ↓
Google возвращает credential (id_token)
        ↓
Фронтенд (Vercel) отправляет token на бэкенд
POST https://ВАШ.onrender.com/api/v1/auth/google/
        ↓
Бэкенд (Render/Django) проверяет token через Google
        ↓
Создаёт или находит пользователя в БД
        ↓
Возвращает JWT токен
        ↓
Пользователь залогинен ✅
```
